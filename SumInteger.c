//operation of sum between two integers
#include <stdio.h>

int main() {
    int n1, n2, sum;

    printf("Input put 2 numbers:- ");
    scanf("%d, %d", n1,n2);

    sum = n1 + n2;
    
    printf("Result is: %d\n", sum);
    return 0;
}