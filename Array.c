#include <stdio.h>
//array = a data structure that can store many values of the same data type
int main() {
    double prices[] = {5.0, 4.0, 3.0, 2.0, 1.0};
    char names[] [10] = {"Luqman", "Nurhakim", "Bin", "Rosli"};
    
   


  for (int i = 0; i < && i   ; i++)
  {
      printf("RM%.2lf \n", prices[i]);
      printf("Hello %s ", names[i]);

  }

    return 0;
}
