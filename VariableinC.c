#include <stdio.h>

int main() {

    int x; //variable
    x = 100; //initialization
    int y = 90; //variable + initialization

    int age = 20; //intitger (%d)
    float average = 3.5; //float of points numbers (%f)
    char grade = 'A'; //single character (%c)
    char name[] = "Luqman"; //array of characters (%s)

    printf("My name is %s.\n", name);
    printf ("Im age right now is %d.\n", age);
    printf("Average of my cgpa is %f.\n", average);
    printf("My favourite subject i got %c." grade);

    return 0;

}